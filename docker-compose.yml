version: '3.8'

services:
  prosbc-nap-ui:
    build: .
    ports:
      - "80:80"
    restart: unless-stopped
    container_name: prosbc-nap-ui

  # Optional: If you want to add SSL later
  # nginx-proxy:
  #   image: nginxproxy/nginx-proxy
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - /var/run/docker.sock:/tmp/docker.sock:ro
